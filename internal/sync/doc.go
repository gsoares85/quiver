// Package sync implements the optional real-time collaboration layer: a
// self-hostable relay that brokers opaque CRDT (Yjs) updates between peers, and the
// persistence step that materializes converged state back into git-native files.
package sync
