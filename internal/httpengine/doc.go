// Package httpengine executes resolved requests against real endpoints — HTTP,
// WebSocket, gRPC, and GraphQL — with streaming responses, accurate timing, auth,
// and full context-based cancellation. It has no knowledge of storage, scripting,
// or the UI.
package httpengine
