// Package model defines Quiver's domain entities — Workspace, Collection, Folder,
// Request, Environment, Variable, Auth, Body, Example, and the runtime Response —
// as pure data types with validation and no I/O. It is the shared vocabulary every
// other package in the engine speaks.
package model
