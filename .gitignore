# Build artifacts
dist/
*.exe
*.out
*.tsbuildinfo

# Go test coverage
/cover.out
/coverage.txt

# Node / frontend
node_modules/

# Wails generated output and JS bindings (regenerated on build)
/apps/desktop/build/
/apps/desktop/frontend/wailsjs/
package.json.md5

# Stray Go build binaries (prefer `go build -o` / `wails build`)
/desktop
/quiver

# Local, machine-specific runtime state — never versioned
.quiver/

# OS / editor cruft
.DS_Store
